import type { BridgetSettings } from './types';

export const DEFAULT_SETTINGS: BridgetSettings = {
  fontSize: 1.5,
  fontFamily: "system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif",
  textColor: '#ffffff',
  bgOpacity: 0.75,
  subtitlePosition: 'bottom',
  highContrast: false,
  reducedMotion: false,
};
